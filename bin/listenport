#!/usr/bin/env bash
# Usage: listenport <port>
# Show processes using the given TCP/UDP port (lsof).
if [ -z "$1" ]; then
  echo "Usage: listenport <port>" >&2
  exit 1
fi

out=$(lsof -nP -i ":$1" 2>/dev/null)
if [ -z "$out" ]; then
  echo "Nothing using port $1"
  exit 0
fi

printf '%s\n' "$out"
